import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.185.1/build/three.module.js';
import {EffectComposer} from 'https://cdn.jsdelivr.net/npm/three@0.185.1/examples/jsm/postprocessing/EffectComposer.js';
import {RenderPass} from 'https://cdn.jsdelivr.net/npm/three@0.185.1/examples/jsm/postprocessing/RenderPass.js';
import {UnrealBloomPass} from 'https://cdn.jsdelivr.net/npm/three@0.185.1/examples/jsm/postprocessing/UnrealBloomPass.js';
import {OutputPass} from 'https://cdn.jsdelivr.net/npm/three@0.185.1/examples/jsm/postprocessing/OutputPass.js';

const loading=document.querySelector('#loading');
const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});
renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight);
renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.03;
document.body.appendChild(renderer.domElement);
const scene=new THREE.Scene();scene.fog=new THREE.FogExp2(0x071321,.0105);
const camera=new THREE.PerspectiveCamera(48,innerWidth/innerHeight,.1,550);camera.position.set(0,7.5,31);camera.lookAt(0,10.5,-78);
function hash(x,z){const s=Math.sin(x*127.1+z*311.7)*43758.5453;return s-Math.floor(s)}
function sm(t){return t*t*(3-2*t)}
function noise(x,z){const ix=Math.floor(x),iz=Math.floor(z),fx=x-ix,fz=z-iz,u=sm(fx),v=sm(fz),a=hash(ix,iz),b=hash(ix+1,iz),c=hash(ix,iz+1),d=hash(ix+1,iz+1);return THREE.MathUtils.lerp(THREE.MathUtils.lerp(a,b,u),THREE.MathUtils.lerp(c,d,u),v)}
function fbm(x,z){let s=0,a=.5,f=1;for(let i=0;i<5;i++){s+=noise(x*f,z*f)*a;f*=2.03;a*=.5}return s}
function seeded(i,k=1){const v=Math.sin(i*12.9898*k+78.233)*43758.5453;return v-Math.floor(v)}
function groundY(x,z){return-2.4+(fbm(x*.031,z*.031)-.5)*5.2+(fbm((x+53)*.085,(z-21)*.085)-.5)*1.25+Math.pow(Math.min(1,Math.abs(x)/37),1.7)*4.2-Math.exp(-(x*x)/220)*1.55+THREE.MathUtils.clamp((-z-30)/125,0,1)*2.4}
function gaussian(){return Math.random()+Math.random()+Math.random()+Math.random()-2}
function makeSky(){const W=2048,H=1152,c=document.createElement('canvas');c.width=W;c.height=H;const x=c.getContext('2d'),g=x.createLinearGradient(0,0,0,H);g.addColorStop(0,'#01030b');g.addColorStop(.3,'#06102a');g.addColorStop(.6,'#10264d');g.addColorStop(.8,'#183657');g.addColorStop(1,'#091522');x.fillStyle=g;x.fillRect(0,0,W,H);x.globalCompositeOperation='screen';for(let i=0;i<620;i++){const t=Math.random(),cx=W*(-.07+1.15*t),cy=H*(.79-.67*t+Math.sin(t*8.4)*.021),off=gaussian()*H*.09,px=cx+.48*off,py=cy+.88*off,r=22+Math.random()*92,rg=x.createRadialGradient(px,py,0,px,py,r);rg.addColorStop(0,`rgba(125,157,255,${.02+Math.random()*.055})`);rg.addColorStop(1,'rgba(30,55,130,0)');x.fillStyle=rg;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}for(let i=0;i<3800;i++){const t=Math.random(),cx=W*(-.04+1.11*t),cy=H*(.79-.67*t),off=gaussian()*H*.05,px=cx+.48*off,py=cy+.88*off,cw=Math.exp(-Math.pow((t-.55)/.23,2)),r=.7+Math.random()*(3.2+cw*6),a=.014+Math.random()*(.07+cw*.07),rg=x.createRadialGradient(px,py,0,px,py,r);rg.addColorStop(0,`rgba(${170+Math.floor(45*cw)},${190+Math.floor(35*cw)},255,${a})`);rg.addColorStop(1,'rgba(90,110,220,0)');x.fillStyle=rg;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}x.globalCompositeOperation='source-over';for(let i=0;i<470;i++){const t=Math.random(),cx=W*(-.04+1.11*t),cy=H*(.79-.67*t),off=gaussian()*H*.02+H*.01,px=cx+.48*off,py=cy+.88*off,r=11+Math.random()*44,rg=x.createRadialGradient(px,py,0,px,py,r);rg.addColorStop(0,`rgba(0,3,14,${.03+Math.random()*.08})`);rg.addColorStop(1,'rgba(0,2,10,0)');x.fillStyle=rg;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}x.globalCompositeOperation='screen';for(let i=0;i<7600;i++){const px=Math.random()*W,py=Math.pow(Math.random(),1.06)*H*.82,b=Math.random(),r=b>.997?1.4+Math.random()*1.4:.22+Math.random()*.7,a=b>.997?.9:.13+Math.random()*.57;x.fillStyle=`rgba(225,235,255,${a})`;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;t.anisotropy=renderer.capabilities.getMaxAnisotropy();return t}
scene.background=makeSky();
function disc(){const c=document.createElement('canvas');c.width=c.height=512;const x=c.getContext('2d'),g=x.createRadialGradient(256,256,8,256,256,250);g.addColorStop(0,'rgba(255,255,248,1)');g.addColorStop(.13,'rgba(244,250,255,.98)');g.addColorStop(.28,'rgba(190,216,255,.35)');g.addColorStop(.6,'rgba(110,150,255,.09)');g.addColorStop(1,'rgba(90,130,255,0)');x.fillStyle=g;x.fillRect(0,0,512,512);return new THREE.CanvasTexture(c)}
const moon=new THREE.Sprite(new THREE.SpriteMaterial({map:disc(),transparent:true,depthWrite:false,blending:THREE.AdditiveBlending,fog:false}));moon.position.set(37,32,-130);moon.scale.set(7.4,7.4,1);scene.add(moon);
function groundTex(){const N=512,c=document.createElement('canvas');c.width=c.height=N;const x=c.getContext('2d');x.fillStyle='#142019';x.fillRect(0,0,N,N);for(let i=0;i<1500;i++){const px=Math.random()*N,py=Math.random()*N,r=3+Math.random()*30,rg=x.createRadialGradient(px,py,0,px,py,r),green=Math.random()>.5;rg.addColorStop(0,green?`rgba(31,65,40,${.05+Math.random()*.14})`:`rgba(68,53,34,${.04+Math.random()*.11})`);rg.addColorStop(1,'rgba(0,0,0,0)');x.fillStyle=rg;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(16,20);t.anisotropy=renderer.capabilities.getMaxAnisotropy();return t}
const tg=new THREE.PlaneGeometry(140,180,150,170);tg.rotateX(-Math.PI/2);const pp=tg.attributes.position;for(let i=0;i<pp.count;i++){const x=pp.getX(i),z=pp.getZ(i)-52;pp.setZ(i,z);pp.setY(i,groundY(x,z))}tg.computeVertexNormals();scene.add(new THREE.Mesh(tg,new THREE.MeshStandardMaterial({map:groundTex(),color:0x718176,roughness:1,metalness:0})));
function ridge(z,base,amp,width,col,seed){const n=180,pos=[];for(let i=0;i<n-1;i++){const f0=i/(n-1),f1=(i+1)/(n-1),x0=-width/2+width*f0,x1=-width/2+width*f1,y0=base+Math.sin(f0*10.7+seed)*amp*.22+(fbm(f0*8+seed,seed*.3)-.5)*amp,y1=base+Math.sin(f1*10.7+seed)*amp*.22+(fbm(f1*8+seed,seed*.3)-.5)*amp;pos.push(x0,y0,z,x1,y1,z,x0,-20,z,x1,y1,z,x1,-20,z,x0,-20,z)}const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.Float32BufferAttribute(pos,3));g.computeVertexNormals();scene.add(new THREE.Mesh(g,new THREE.MeshStandardMaterial({color:col,roughness:1})))}
ridge(-142,6,7.2,205,0x183552,2.2);ridge(-171,8.2,9.3,235,0x102943,4.7);ridge(-205,10.3,11.5,270,0x0a1f35,8.1);
scene.add(new THREE.HemisphereLight(0x8ba7cf,0x07100b,.56));const key=new THREE.DirectionalLight(0xb9d2ff,1.45);key.position.set(45,60,30);key.target.position.set(-5,2,-70);scene.add(key,key.target,new THREE.AmbientLight(0x0b1830,.19));
const loader=new THREE.TextureLoader();loader.setCrossOrigin('anonymous');
const TREE='https://cdn.polyhaven.com/asset_img/thumbs/fir_tree_01.png?format=png&width=1500';
const ROCK='https://cdn.polyhaven.com/asset_img/renders/rock_moss_set_01/orth_front.png?height=900&quality=95';
function load(url){return new Promise((res,rej)=>loader.load(url,t=>{t.colorSpace=THREE.SRGBColorSpace;t.anisotropy=renderer.capabilities.getMaxAnisotropy();res(t)},undefined,rej))}
function crop(base,x,w){const t=base.clone();t.needsUpdate=true;t.offset.set(x,0);t.repeat.set(w,1);return t}
function addTree(map,aspect,x,z,h,tint=0xffffff,opacity=1){const s=new THREE.Sprite(new THREE.SpriteMaterial({map,color:tint,transparent:true,alphaTest:.16,depthWrite:true,opacity,fog:true}));s.center.set(.5,0);s.position.set(x,groundY(x,z)-.04,z);s.scale.set(h*aspect,h,1);scene.add(s);return s}
function addRock(map,x,z,h){const s=new THREE.Sprite(new THREE.SpriteMaterial({map,color:0x89938c,transparent:true,alphaTest:.12,depthWrite:true,fog:true}));s.center.set(.5,.07);s.position.set(x,groundY(x,z)+.02,z);s.scale.set(h*1.18,h,1);scene.add(s)}
function fogTex(){const c=document.createElement('canvas');c.width=512;c.height=256;const x=c.getContext('2d');for(let i=0;i<30;i++){const px=70+Math.random()*370,py=70+Math.random()*120,r=30+Math.random()*100,g=x.createRadialGradient(px,py,0,px,py,r);g.addColorStop(0,`rgba(150,184,220,${.025+Math.random()*.04})`);g.addColorStop(1,'rgba(95,130,180,0)');x.fillStyle=g;x.beginPath();x.arc(px,py,r,0,Math.PI*2);x.fill()}return new THREE.CanvasTexture(c)}
const ft=fogTex(),fog=[];for(let i=0;i<24;i++){const z=-22-seeded(i+900,1.7)*104,x=(seeded(i+900,2.1)-.5)*56,m=new THREE.SpriteMaterial({map:ft,transparent:true,depthWrite:false,opacity:.16+seeded(i+900,4)*.11,color:0xa9c6e4,fog:true}),s=new THREE.Sprite(m);s.position.set(x,groundY(x,z)+1.2+seeded(i+900,3)*2,z);s.scale.set(14+seeded(i+900,5)*25,4+seeded(i+900,6)*5,1);s.userData={x,phase:seeded(i+900,7)*6.28};scene.add(s);fog.push(s)}
try{const [tb,rb]=await Promise.all([load(TREE),load(ROCK)]);const tm=[crop(tb,0,.39),crop(tb,.335,.36),crop(tb,.665,.335)],ta=[.365,.337,.313],rm=[crop(rb,0,.34),crop(rb,.33,.34),crop(rb,.66,.34)];for(let i=0;i<105;i++){const z=-50-seeded(i,1.2)*80;let x=(seeded(i,2.4)-.5)*102;const gap=10+THREE.MathUtils.clamp((z+125)/80,0,1)*8;if(Math.abs(x)<gap&&z>-112)x+=Math.sign(x||1)*(gap+seeded(i,8)*6);const idx=Math.floor(seeded(i,3.2)*3);addTree(tm[idx],ta[idx],x,z,4.7+seeded(i,4.7)*7,0x78919e,.78+seeded(i,5.4)*.14)}const hero=[[-24,7,18,0],[-19,-4,13,1],[-29,-14,16,2],[-22,-26,12,0],[25,5,13,2],[31,-7,16,0],[22,-20,11,1],[36,-33,13,2],[-37,-36,14,1]];hero.forEach(v=>addTree(tm[v[3]],ta[v[3]],v[0],v[1],v[2],0x81999d,1));const rocks=[[-17,9,3.1,0],[-8,2,1.9,2],[14,8,3,1],[23,-3,2.2,0],[-25,-13,2.5,2],[27,-18,2.2,1],[7,-8,1.4,2],[-4,11,1.3,1]];rocks.forEach(v=>addRock(rm[v[3]],v[0],v[1],v[2]));loading.innerHTML='CINEMATIC PASS 04 READY';setTimeout(()=>loading.style.opacity='0',900)}catch(e){console.error(e);loading.innerHTML='POLY HAVEN CDN FAILED<br><span style="opacity:.5">reload with internet access</span>'}
const composer=new EffectComposer(renderer);composer.addPass(new RenderPass(scene,camera));composer.addPass(new UnrealBloomPass(new THREE.Vector2(innerWidth,innerHeight),.15,.45,.97));composer.addPass(new OutputPass());const clock=new THREE.Clock();function animate(){requestAnimationFrame(animate);const t=clock.getElapsedTime();fog.forEach((s,i)=>{s.position.x=s.userData.x+Math.sin(t*.035+s.userData.phase)*.9});camera.position.x=Math.sin(t*.028)*.18;camera.position.y=7.5+Math.sin(t*.023)*.05;camera.lookAt(Math.sin(t*.018)*.2,10.5,-78);composer.render()}animate();
addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight);composer.setSize(innerWidth,innerHeight)});